version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081
  metabase:
    image: metabase/metabase
    restart: always
    ports:
      - 3000:3000
